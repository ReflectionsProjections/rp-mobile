import React from 'react';
import { View, Text } from 'react-native';

const EventsScreen = () => (
  <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
    <Text style={{color: '#f0f0f0'}}>Events Screen</Text>
  </View>
);

export default EventsScreen;
